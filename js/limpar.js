export function limpar(valorAnterior, resultado, operador, operadorEmAndamento) {
    const display = document.querySelector("p");
    display.textContent = "0";
    valorAnterior = 0;
    resultado = 0;
    operador = "";
    operadorEmAndamento = false;
}

